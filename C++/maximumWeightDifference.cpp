#include <iostream>
#include <algorithm>

using namespace std;

int main(int argc char* argv[]) {
    int t;
    cin >> t;
    while (t--) {
        int n, k, weights[100000];
        cin >> n >> k;
        for (int i = 0; i < n; i++) {
            cin >> weights[i];
        }
        
    }
}